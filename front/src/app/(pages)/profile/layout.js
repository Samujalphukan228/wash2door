import { AuthProvider } from '@/context/AuthContext'

export const metadata = {
  title: 'My Profile | Wash2Door',
  description: 'Manage your Wash2Door account',
}

export default function ProfileLayout({ children }) {
  return children
}